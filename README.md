# fesTerm

A scratch implementation of a multi-platform SSH/terminal program, written in Rust.

## Status

Early scaffolding — work in progress. See [OUTLINE.md](OUTLINE.md) for the
project outline; a full design document will follow.

## Building

Requires a Rust toolchain (via [rustup](https://rustup.rs/)).

```sh
cargo build
cargo run
```
