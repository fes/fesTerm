# esctest2 tests that `esctest2-allow.txt` enables but that we exclude, with
# the reason for each. One reason per line, so every skip is an admission
# rather than a silence.
#
# Entries are Python regexes matched against the full test name. A skip wins
# over the allowlist.
#
# "We have not got round to it" does not belong here - that is the
# allowlist's absence, and #193 tracks the phases. This is for tests we have
# deliberately pulled *out* of a family we otherwise hold ourselves to.

# --- Blocked on DECRQCRA (#193 phase 2) ---
# These assert screen contents, which esctest can only read through DECRQCRA.
# Until we answer it they cannot observe anything, pass or fail.
CHATests\.test_CHA_RespectsOriginMode
CUPTests\.test_CUP_RespectsOriginMode
HVPTests\.test_HVP_RespectsOriginMode
CRTests\.test_CR_MovesToLeftMarginWhenLeftOfLeftMarginInOriginMode
DECRCTests\.test_SaveRestoreCursor_InsertNotAffected
DECRCTests\.test_SaveRestoreCursor_Protection
DECRCTests\.test_SaveRestoreCursor_Reset
DECRCTests\.test_SaveRestoreCursor_ResetsOriginMode
DECSETTiteInhibitTests\.test_SaveRestoreCursor_InsertNotAffected
DECSETTiteInhibitTests\.test_SaveRestoreCursor_Protection
DECSETTiteInhibitTests\.test_SaveRestoreCursor_Reset
DECSETTiteInhibitTests\.test_SaveRestoreCursor_ResetsOriginMode
SCORCTests\.test_SaveRestoreCursor_InsertNotAffected
SCORCTests\.test_SaveRestoreCursor_Protection
SCORCTests\.test_SaveRestoreCursor_Reset
SCORCTests\.test_SaveRestoreCursor_ResetsOriginMode

# --- Blocked on left/right margins, DECSLRM and DECSET 69 (#193 phase 5) ---
# We have no left/right margin in the screen model, so the cursor has nothing
# to stop at and these all measure against the screen edge instead.
CNLTests\.test_CNL_StopsAtBottomLineWhenBegunBelowScrollRegion
CNLTests\.test_CNL_StopsAtBottomMarginInScrollRegion
CPLTests\.test_CPL_StopsAtTopLineWhenBegunAboveScrollRegion
CPLTests\.test_CPL_StopsAtTopMarginInScrollRegion
CRTests\.test_CR_MovesToLeftMarginWhenRightOfLeftMargin
CRTests\.test_CR_StaysPutWhenAtLeftMargin
CUBTests\.test_CUB_StopsAtLeftMarginInScrollRegion
CUFTests\.test_CUF_StopsAtRightMarginInScrollRegion
SCORCTests\.test_SaveRestoreCursor_WorksInLRM

# --- Blocked on reverse wraparound, DECSET 45 ---
# Backspace past the left edge does not climb to the previous line. Not in
# any phase of #193 yet; it needs a decision first, because reverse wrap is
# only well defined when the terminal remembers which rows soft-wrapped.
CUBTests\.test_CUB_AfterNoWrappedInlines
CUBTests\.test_CUB_AfterOneWrappedInline

# --- Known defects, found by this harness ---
# CUD and CUU do not clamp to the scroll region when the cursor starts inside
# it: with DECSTBM 2;4 and the cursor on row 3, CUD 99 lands on row 25 and
# CUU 99 on row 1, instead of rows 4 and 2. Tracked by #206.
CUDTests\.test_CUD_StopsAtBottomMarginInScrollRegion
CUUTests\.test_CUU_StopsAtTopMarginInScrollRegion
# Resetting DECAWM does not clear the pending-wrap flag, so a restore leaves
# the cursor one column right of where DEC STD 070 puts it. Tracked by #207.
DECRCTests\.test_SaveRestoreCursor_Wrap
DECSETTiteInhibitTests\.test_SaveRestoreCursor_Wrap
# Restoring with nothing saved should home the cursor; we leave it where it
# is. DECSTR (`CSI ! p`) is also unimplemented, so the saved cursor survives
# a soft reset and leaks from one test into the next. Tracked by #208.
DECRCTests\.test_SaveRestoreCursor_MoveToHomeWhenNotSaved
DECSETTiteInhibitTests\.test_SaveRestoreCursor_MoveToHomeWhenNotSaved
SCORCTests\.test_SaveRestoreCursor_MoveToHomeWhenNotSaved
